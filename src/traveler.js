class Taveler {
  constructor(client) {
    this.past = client.past;
    this.present = client.present;
    this.upcoming = client.upcoming;
    this.pending = client.pending;
  }
  calculateFees() {
    
  }
}

export default User;
